# app-ads.txt — AdMob 승인 판매자 선언 (광고 사기 방지, IAB 표준)
#
# 이 파일은 스토어 등록정보의 "개발자 웹사이트" 로 등록한 **루트 도메인**에서
#   https://<도메인>/app-ads.txt
# 로 접근 가능해야 한다. 서브도메인에 두면 크롤러가 못 찾는다 — 크롤러는
# 개발자 웹사이트 URL 의 루트 도메인만 본다.
#
# 배포는 tool/build_web.sh 가 Flutter 웹 빌드 위에 이 폴더를 얹어 루트로 서빙한다.
# (웹은 서버에 쓰지 않는 데모 모드로 돈다 — lib/config/app_mode.dart 참고.)
#
# 퍼블리셔 pub-4235844602701475 는 iOS/Android 공통(AdMob 계정 단위).
google.com, pub-4235844602701475, DIRECT, f08c47fec0942fa0
